# Multi-tenant worker configuration
# Client configs live under config/clients/{client-id}/agent.properties
# Tenant routing uses telephony.phone_number on each client config.

agent.name=voice-agent
default.client=client-1
